{% extends 'base.html.twig' %}

{# {% form_theme contactform %} #}

{% block title %}My Contacts{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center text-primary">Edit {{contact.firstname}}'s Contact</h2>
        <br />
        {{ include ('contacts/common/contact-form.html.twig', {contactform: contactform, button: 'Update'}) }}
    </div>
{% endblock %}